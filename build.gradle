plugins {
	id 'org.hidetake.ssh' version '2.9.0'
}

repositories {
    jcenter()
    maven {
    	url "http://192.168.255.11/maven2"
    }
}

remotes {
  webServer {
    host = '192.168.255.11'
    user = 'administrator'
    password = 'viadesk'
  }
}

ssh.settings {
  timeoutSec = 10
}

task deploy {
  doLast {
    ssh.run {
      session(remotes.webServer) {
        executeSudo 'systemctl restart tomcat8-suite', pty: true
      }
    }
  }
}